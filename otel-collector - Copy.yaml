receivers:
  otlp:
    protocols:
      grpc:
       endpoint: otel-collector:4317
      http:

processors:
  batch:

exporters:
  prometheus:
    endpoint: otel-collector:8889

service:
  pipelines:
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [prometheus]
